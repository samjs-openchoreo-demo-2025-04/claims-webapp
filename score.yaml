apiVersion: score.dev/v1b1
metadata:
  name: claims-webapp
  project: internal-apps
  organization: claims-webapp
service:
  ports:
    default:
      port: 8080
      protocol: TCP
containers:
  container-one1:
    image: .